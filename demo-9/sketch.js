function setup() {
  createCanvas(window.innerWidth, window.innerHeight);
  background(0);
}

function draw() {}

class Synble {
  constructor(x, y) {
    this.x = x;
    this.y = y;
    this.value;

    this.setToRandomSymbol = function () {};
  }
}
